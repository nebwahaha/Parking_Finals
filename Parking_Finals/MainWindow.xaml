<Window x:Class="Parking_Finals.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Parking_Finals"
        mc:Ignorable="d"
        Title="MainWindow" Height="300" Width="400">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Username Label -->
        <Label Content="Username:" Grid.Row="0" Grid.Column="0" Margin="10"/>

        <!-- Username TextBox -->
        <TextBox Name="txtbusername" Grid.Row="0" Grid.Column="1" Margin="10"/>

        <!-- Password Label -->
        <Label Content="Password:" Grid.Row="1" Grid.Column="0" Margin="10"/>

        <!-- Password TextBox -->
        <PasswordBox Name="txtbpass" Grid.Row="1" Grid.Column="1" Margin="10"/>

        <!-- Login Button -->
        <Button Content="Login" Grid.Row="2" Grid.Column="0" Margin="10" Click="btnLogin_Click"/>
    </Grid>
</Window>
